# Puter.js Configuration
# No environment variables needed - Puter.js handles authentication automatically

# Production environment
NODE_ENV=production

# Optional: Add your domain for CORS if needed
# ALLOWED_ORIGINS=https://your-domain.vercel.app